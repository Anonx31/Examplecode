<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>CSS Positioning Example</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: Arial;
    }

    .container {
      position: relative;
      height: 400px;
      background-color: #f0f0f0;
      border: 2px dashed #aaa;
      margin: 40px;
    }

    .static-box {
      position: static;
      background-color: lightblue;
      padding: 10px;
      margin: 10px;
    }

    .relative-box {
      position: relative;
      background-color: lightgreen;
      padding: 10px;
      top: 20px;
      left: 30px;
    }

    .absolute-box {
      position: absolute;
      top: 50px;
      right: 20px;
      background-color: coral;
      padding: 10px;
    }

    .fixed-box {
      position: fixed;
      top: 10px;
      right: 10px;
      background-color: gold;
      padding: 10px;
      box-shadow: 0 0 5px #888;
    }
  </style>
</head>
<body>

  <div class="fixed-box">Fixed (Always on screen)</div>

  <div class="container">
    <div class="static-box">Static (default)</div>
    <div class="relative-box">Relative (offset by 20px down, 30px right)</div>
    <div class="absolute-box">Absolute (inside container)</div>
  </div>

  <p style="margin: 1000px 20px;">Scroll down to see the fixed box stays in place.</p>

</body>
</html>
